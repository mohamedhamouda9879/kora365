import 'package:flutter/material.dart';

class ScoorScreen extends StatelessWidget {
  const ScoorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
